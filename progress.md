# Progress tracker

Status legenda: ❌ neumím / 🟡 částečně / ✅ pohoda
Sebehodnocení: 1–10 (1 = vidím poprvé, 10 = vysvětlím komukoli)

---

## SWI (25 témat)

| # | Téma | Sebehodn. (před) | Sebehodn. (po) | Status | Poslední revize | Poznámka |
|---|------|---|---|---|---|---|
| 1 | Diagramy UML | 4 | 7.5 | ✅ | 2026-05-17 | Heslovitý + 3 follow-upy. Pokrytí 5/6 keywords Popisu v papírku, **Use Case úplně chyběl** ❌ ale po doptání kompletně zachráněn ✅✅. **Silné momenty:** **Visibility 4 modifiers** (+/-/#/~) ⭐, **kardinalita s porovnáním ER** (* / 1 / 1..5 vs 1:1/1:N/M:N) ⭐, vztahy s explicit symboly (prázdný × plný kosočtverec), Q1 include/extend s konkrétním příkladem (objednávka+přihlášení=include, zapomenuté heslo=extend) ⭐. Q2 sequence: lifeline + activation bar + sync/async + 3 combined fragments (alt/opt/loop) ✅. Q3 Activity: swimlanes ✅, paralelní princip ✅. **Drill body:** 1) **"Případy užití" = Use Case** (synonyma! v Popisu vždy spojit). 2) **Fork/Join v Activity** = tlustá čára ▬ (paralelní rozdělení/sloučení), v zápiscích to JE, přehlédl. 3) Sync zpráva = plná šipka, async = otevřená šipka. 4) Combined fragments = řízení toku (ne "zápis algoritmu"). 5) Generalization v Use Case mezi aktéry/use cases (nezmíněno). 6) OMG + 14 typů ve 2 kategoriích (strukturální/behaviorální) v úvodu. User push-back: "Use case se nepřekládá jako případy užití?" — fér, jsou synonyma. |
| 2 | Algoritmus | 3 | 8 | ✅ | 2026-05-10 (3× drill) | Re-drill heslovitý formát s preemptivním "algoritmus × kód" rozdílem. **Vlastnosti: 6/7** (efektivita, hromadnost, determinovanost, správnost, vstup/výstup, výslednost). **Chybí KONEČNOST** — drill 7. vlastnost! Zápis kompletně se symboly flowchartu (oval start, kosočtverec rozhodnutí, obdélník akce, kosodelník vstup). Big O 5 tříd + bubble sort příklad. Mini B: vnořené cykly = O(n²) ✅. |
| 3 | Reprezentace dat | 4 | 7 | ✅ | 2026-05-13 | Heslovitý + 3 follow-upy. Pokrytí 5/5 bodů Popisu, **silné konverze**: binární↔dec přes součet/odečítání mocnin (`101100₂ = 32+8+4 = 44`), hex↔dec přes binární mezikrok (`0x1A = 00011010 = 16+8+2 = 26`). Bonus: 60-systém kulturní mention, 2038 problem znal. String správně identifikován jako složený + referenční + imutabilní (po opravě — původně řekl základní). **Doplněno: float precision** — binární floating-point IEEE 754 nemůže přesně reprezentovat 0.1 (jako desítková 1/3), proto `0.1+0.2 != 0.3`. Pro peníze `decimal`. **Mantra:** *"float/double pro fyziku, decimal pro peníze"*. NIKDY ne `==` na floaty — `Math.Abs(a-b) < epsilon`. |
| 4 | Datové typy, proměnné | 5 | 8 | ✅ | 2026-05-09 (re-drill) | Re-drill heslovitý formát zavřel všech 7 bodů Popisu solidně. Imutabilita s string+GC příkladem ✅, lístek/dům aplikovaný ✅, všechny 3 druhy přetypování s C# příklady ✅. **Vlastní rozšíření metafory:** "smart home, dva přístupy, b zapne topení → a vidí" — silnější než lístek/dům, použít u zkoušky. Drobnost: funkční scope vs. třídní scope splývaly. |
| 5 | Návrhové vzory | 2 | 7 | ✅ | 2026-05-17 | Heslovitý + 3 follow-upy. Pokrytí Popisu kompletní (4 vzory dle Prchala + 3 kategorie + Singleton + MVC distinction). **Silné momenty:** **MVC distinction** ⭐ explicit ("MVC NENÍ návrhový vzor, je to typ architektury"), race condition u Singletonu, 3 kategorie definované precizně, **Command kategorie opravena okamžitě v komise módu** ⭐ (z papírku měl strukturální → opraveno na behaviorální + vysvětlení proč). **Drill body:** 1) **Gang** of Four (ne "Group"). 2) Command = behaviorální. 3) Strukturální vzory: Adapter, Composite, Facade, Decorator, Proxy. 4) Behaviorální (kromě Observer/Command): Strategy, State, Template Method. 5) Singleton thread-safe: lock, double-checked locking, eager init, Lazy<T>, enum (chce 3+). 6) Factory Method: chybí konkrétní příklad cross-platform UI. 7) Singleton: chybí privátní ctor + getInstance + anti-pattern crítique. User push-back o "příklady nebyly v zápiscích" → byly v Prchalu i mých notes (Adapter/Composite/Facade), přehlédl. |
| 6 | Chyby, testování, ladění | 3 | 7 | ✅ | 2026-05-17 | Heslovitý + 3 follow-upy. Pokrytí 4/4 keywords Popisu (identifikace chyb, výjimky, debugging, testy). **Silné momenty:** 4 druhy chyb precizně definované s příklady (string→int, neexistující soubor), pořadí catch specifický→obecný, "narozdíl od chybových kódů, které proběhnou tiše" ⭐ (chápe rozdíl výjimky vs návratové kódy), 3 typy stepping (Into/Out/Over). **Q1a race condition + mutex** ⭐ bonus pochopení. **Drobnosti k drillu:** 1) terminologický slip — "debugging" použil pro sekci o testech (správně je ladění = breakpoint, watch, stepping). 2) "mock místo produkčních" → správně "místo reálných závislostí". 3) **Q1b chybný předpoklad:** single-thread NENÍ race-condition-free při I/O (file system, DB, network = sdílené resources mezi procesy). 4) Propagace výjimky stack → unhandled exception handler v runtime → app crash + stack trace. 5) Pyramida testů důvody: nejen čas, ale i křehkost + lokalizace chyby + údržba. 6) Mock důvody: rychlost + izolace + **determinismus**. |
| 7 | Šifrování a kódování | 5 | 7 | ✅ | 2026-05-17 | Heslovitý + 3 follow-upy. Pokrytí 4/4 keywords. **Silné momenty:** kódování × šifrování distinction "NEUTAJUJE" ⭐, hashing kompletní (deterministické/lavinové/NELZE ZPĚT + SHA-256 + Argon + salt + rainbow tables), metafora barev pro DH přesně, hybrid HTTPS, **obě faktické chyby z papírku opraveny okamžitě v komise módu** ⭐⭐. **2 faktické chyby v papírku:** 1) Transpozice × substituce definice prohozené (obě = substituce). 2) AES/DH označeny za asymetrické — AES je SYMETRICKÉ, DH je VÝMĚNA KLÍČŮ, asymetrické jsou RSA/ECC (které nezmínil). **Q3 digitální podpis:** 2/3 vlastností (autenticita ✅, integrita ❌ chybí, nepopiratelnost ❌ chybí), klíčový obrat privát↔veřej ✅⭐, hash zprávy ✅. **Drill body:** 1) TransPOZICE = POZICE (pořadí), SUBStituce = písmena. 2) AES sym, RSA/ECC asym, DH výměna klíčů. 3) Digitální podpis = autenticita + **integrita** + nepopiratelnost. 4) Vigenère, Turing (pravopis). |
| 8 | Kryptosystémy | 4 | 8 | ✅ | 2026-05-17 | **Pivot na pure Q&A** (user navrhl) — papírek skipnut kvůli overlapu se SWI 7, soustředění na unique SWI 8 obsah. **Silné momenty:** **4 vlastnosti kryptosystému** ⭐ s mechanismy (důvěrnost=šifr, autenticita=podpis+cert, integrita=hash, nepopiratelnost=podpis) — velký skok od SWI 7 Q3 (kde zmínil jen autenticitu). **Kerckhoffsův princip** precizně. **ECB tučňák** vysvětlen perfektně. **GCM** = counter + autentizace. **Chain of trust** Root CA→Let's Encrypt→terpino. **MITM obrana** certifikátem. **Drobnosti k drillu:** 1) **Veřejný klíč** = klíčová součást certifikátu (zmínil jen metadata). 2) Root CA preinstalovaný v OS/prohlížeči. 3) PFS (Forward Secrecy) — efemérní DH per spojení, TLS 1.3 povinné. 4) Post-quantum (Shorův algoritmus, NIST 2024 Kyber/Dilithium). 5) HSTS vynucuje HTTPS. Lepší hodnocení než SWI 7 (8 > 7/10) díky pochopení celého ekosystému. |
| 9 | OOP | 5 | 8 | ✅ | 2026-05-15 (re-drill) | **Re-drill úspěšný — obě reverzní chyby OPRAVENÉ:** (1) polymorfismus overload=compile-time, override=runtime ✅; (2) field=raw přístup, property=validační logika ✅. Pokrytí 9/11 keywords Popisu. **Mantra "abstrakce CO JE × interface CO DĚLÁ"** zachována od minula. **4 pilíře explicitně** (polymorfismus, enkapsulace, dědičnost, abstrakce) + komentář "3 nebo 4". **Abstraktní třída zakazuje instanciaci** ⭐. Q1a (konkrétní metody v abstract = DRY/template) ✅. Q1b (multiple: 1 abstract class, N interfaces) ✅. Q2a `base(...)` ✅ princip, ale **drill notační rozdíl: `base(args)` = ctor initializer × `base.Method(args)` = override body**. Q2b sealed ⭐ bonus moment: "compiler může lépe optimalizovat" (JIT inlining). **K doplnění:** konkrétní příklady generik (`List<T>`, `Dictionary<K,V>`), type safety vs Object boxing, `Math.Sqrt` (velké S v C#), interface s default metodami od C# 8+. Gatekeeper Block E **odemčen**. |
| 10 | Databáze | 2 | 7 | ✅ | 2026-05-18 | Heslovitý + 3 follow-upy (po obhajobě, user unavený). Pokrytí 5/5 keywords Popisu, **papírek tenký** ale Q&A solidní. **Silné momenty:** ACID kompletně (všechny 4 vlastnosti), **indexy O(n)→O(log n)** ⭐ Big O notation, transakce přesná definice, **M:N "model neumí, řeší se 2× 1:N přes vazební tabulku"** ⭐, **4 ON DELETE akce** kompletně (RESTRICT/CASCADE/SET NULL/SET DEFAULT) ⭐, NoSQL 4 typy + use cases (Redis/MongoDB/Neo4j/Cassandra). **Drobnost — faktická chyba:** "BT (binary tree)" → správně **B-tree** (Bayer 1972, vícecestný strom pro disk), NE binary tree. **Drill body:** 1) B-tree NE Binary tree. 2) **Referenční integrita** širší definice — "nemůže existovat odkaz na neexistující záznam" (user řekl jen "řeší mazání potomků"). 3) 3 fáze modelování (ER→logický→fyzický) v papírku chybí. 4) Trade-off indexů (rychlejší SELECT, pomalejší INSERT). 5) PK má automatický index. 6) Constraints (NOT NULL/UNIQUE/CHECK/DEFAULT). |
| 11 | Normalizace databáze | 3 | 7 | ✅ | 2026-05-19 | Heslovitý + 3 follow-upy. Pokrytí 5/5 keywords. **Silné momenty:** **3 anomálie kompletní** (Insert/Update/Delete s definicí), **4 normální formy** kompaktně, **tranzitivní závislost identifikována perfektně** (Q2: "dept_id závisí na employee_id, na dept_id závisí dept_name") ⭐, BCNF jádro "rodič MUSÍ BÝT SUPERKLÍČ" ⭐. **Faktická chyba k drillu:** **superklíč × kandidátní klíč OBRÁCENĚ**. User: "kandidátní = něco co identifikuje, superklíč = minimum". Správně: **superklíč = JAKÁKOLI množina (i nadbytečná)**, **kandidátní klíč = MINIMÁLNÍ superklíč**. Mantra: "superklíč může být přebytečný, kandidátní je nejmenší". Drobnosti: 1) Q1b oprava 2NF — v původní tabulce zůstává (order_id, product_id, quantity), ne jen product_id. 2) BCNF v jedné **tabulce**, ne "řádku". 3) OLTP = transakční s konzistencí (ne "přesnější"). |
| 12 | Jazyk SQL | 5 | 7 | ✅ | 2026-05-19 | Heslovitý + 3 follow-upy. Pokrytí 6/6 keywords. **Silné momenty:** **constraints kompletně** všech 6 (PK/FK/UNIQUE/NOT NULL/CHECK/DEFAULT s detaily — PK = NOT NULL + UNIQUE + jen jeden per tabulka, UNIQUE může NULL), **transakce + SAVEPOINT + ROLLBACK** vysvětleno, DCL Grant/Revoke. **Chyby z papírku:** 1) DDL definice popletená ("přidávají/odstraňují data" = DML, ne DDL) — **opraveno v Q1** ✅. 2) **COMMIT chybělo** v papírku transakce, doplněno v Q2. **Drobnost k drillu (ZOPAKOVANÁ chyba Q1c + Q3b):** **TRUNCATE = DDL**, ne DML! User to 2× označil DML. Důvod DDL: pracuje s strukturou/storage (dealokace stránek), nelogguje per řádek, tradičně v DDL family vedle CREATE/ALTER/DROP. **Drill:** 1) TRUNCATE = DDL. 2) TRUNCATE je NEJRYCHLEJŠÍ pro vyprázdnění (žádný per-row log) >> DELETE (musí logovat). 3) DELETE bez WHERE funkčně jako TRUNCATE, ale mnohem pomalejší. 4) Flow transakce: BEGIN → operace → COMMIT/ROLLBACK. SAVEPOINT volitelný. |
| 13 | Internet | 1 | 6 | ✅ | 2026-05-12 | Heslovitý + 3 follow-upy: URL anatomy 8 částí ✅ (rozdělil doménu na sub/SLD/TLD), DNS hierarchie [?] doplněno (4-tier: resolver→root→TLD→authoritative). Drobnosti k drillu: 1) **MIME deklaruje SERVER v HTTP `Content-Type` hlavičce**, NE jako přípona souboru, formát `typ/podtyp` (ne filetype/encoding). 2) DNS = doména → IP (ne opačně, reverse DNS je výjimka). 3) URL pořadí: `protocol://domain:port/path?query#fragment` (port před path). 4) DNS = System, DNS server = jeden prvek systému. |
| 14 | Návrh obsahového webu | 1 | 7 | ✅ | 2026-05-12 | Heslovitý + 2 follow-upy. 5 bodů Popisu pokryto, místy nadprůměr. Workflow 7 fází včetně bezpečnosti, UX/UI crisp, **efektivita s nepřímou souvislostí s SEO** (Core Web Vitals = sofistikovaný pohled). Q1 SEO: bonusy lazy loading, webp/avif, alt kvalita (ne jen přítomnost). Q2 mobile-first: výborná argumentace + výjimky. **Drobnosti k drillu:** [?] druhy viewportů doplněno (layout/visual/ideal + zařízení), SEO doplnit meta description + canonical + Open Graph + HTTPS jako ranking signál. **Mantra:** "Mobile-first není trend, je Google-imposed reality" (mobile-first indexing od 2018). "datová struktura" → správně "informační architektura" web context. SEO správně **Optimization**, ne "Optimalisation". |
| 15 | Webová stránka | 5 | 7 | ✅ | 2026-05-11 | Heslovitý + 3 follow-upy: globální × specifické atributy ✅, struktura ✅, sémantické tagy + 3 důvody ✅. Bonus moment: invisible h1 pro SEO. **Pamatovat:** `<meta name="viewport">` chybělo v Q2 — kritické pro responzivitu, drillni jako "esenciální obsah head". `<body>` je strukturální, ne sémantický. `<article>` definice: "samostatný uzavřený obsah, dá smysl sám o sobě". Materiál v `_materials/swi/15/` byl "šedé" (učitel právem zamítl — tenké), zápisek doplněn z obecných znalostí + překryv s DAT 1. |
| 16 | CSS kaskáda | 3 | 7 | ✅ | 2026-05-10 (re-drill) | Re-drill heslovitý: 3 faktory kaskády (origin/specificita/source order) ✅, `@layer` × `@media` rozlišení ✅ (mantra zafungovala). BEM s reálným příkladem `card__icon--small`. **Korekce v re-drillu:** specificity tuple má 4 kategorie **(inline, ID, [class+attr+pseudo-class], [element+pseudo-element])** — atribut a pseudo-třída se počítají JAKO třída, ne zvlášť. Inline = `style=""` v HTML = `(1,0,0,0)`. `!important` přebíjí celou specificitu. Pamatovat: pravidlo nikdy neposunout sloupce (inline = nejvyšší pozice). |
| 17 | Vlastnosti CSS | 3 | 7 | ✅ | 2026-05-10 | Heslovitý lístek pokryl všech 4 bodů Popisu solidně. Bonus moment: explicitní "em se v em násobí" — komise to ocení. Jednotky, barvy (HEX/RGB/HSL), proměnné s scope (`:root` global × `.card` lokální), písma s woff2/woff/ttf prioritou. Jediný gap: `@font-face` keyword nepojmenován (koncept ale věděl). `font-display: swap` proti FOIT. Drobnost: % u `padding`/`margin` je vždy vůči WIDTH rodiče, i pro vertikální. |
| 18 | React (teorie) | 1 | 7 | ✅ | 2026-05-20 | Heslovitý + 3 follow-upy. Pokrytí 6/6 keywords. **Silné momenty:** DOM × VirtualDOM × ShadowDOM perfektně rozlišené (React nepoužívá Shadow, má CSS Modules) ⭐ — klasický chyták komise zvládnut; transpilace × kompilace přesná definice (citelný↔citelný vs citelný↔strojový) + Babel/SWC s jazyky implementace; JSX = "syntaktický cukr pro HTML do JS" formulace ⭐; `<>` jako Fragment intuitivně pochopen (jméno chybělo); funkcionální komponenty + props imutabilní; **Q1 useState** — princip mimo funkci ✅. **Faktická chyba k drillu (Q2):** **microtask × synchronní kód zaměněn** — `console.log` NENÍ microtask, je to synchronní kód běžící přímo na call stacku. Microtask = `Promise.then`, `queueMicrotask`, `MutationObserver`. Macrotask = `setTimeout`, `setInterval`, DOM events. Pořadí v event loopu: sync doběhne → vyprázdní VŠECHNY microtasks → 1 macrotask → render → opakuj. **Drill body:** 1) JSX musí mít **jeden kořenový element** (proto Fragment existuje). 2) Atributy v JSX **camelCase** (`className`, `onClick`, `htmlFor`). 3) JSX se transpiluje na `React.createElement(...)`. 4) **Vite není dev server**, je to **bundler** (esbuild+Rollup) s dev serverem. 5) **Hooks zmínit u funkcionálních komponent** — useState/useEffect/useContext/useRef jsou důvod proč funkcionální vyhrály nad třídními. 6) Server Components — `"use client"` direktiva pro klient. 7) React Compiler v R19 → automatická memoizace. |
| 19 | Webové aplikace | 1 | 7 | ✅ | 2026-05-13 | Heslovitý + 3 follow-upy. Pokrytí 5/5 bodů, SPA/MPA s frameworky (React/Vue × PHP/Razor), výhody/nevýhody, lazy loading optimalizace. 5 stavových kódů konkrétně. **401 × 403 distinction perfektně:** "401 nevíme kdo, 403 víme kdo ale bez práv". Idempotency definice správně. **K doplnění:** seznam idempotent metod (GET/PUT/DELETE ano, POST ne), porty 80/443 explicitní, HTTPS na port 80 selže přes TLS handshake (ne "nezabezpečený"), HSTS auto-redirect. Chybí PATCH/DELETE/HEAD/OPTIONS v původním papírku (doplněno v Q&A). |
| 20 | Ověřování identity | 1 | 7 | ✅ | 2026-05-13 | Heslovitý + 2 follow-upy. Pokrytí 7/7 (OAuth+OIDC sloučeny do 1 bodu = OK). **Silné momenty:** "random sprinkle" pro salt (vlastní český překlad, účel přesně — proti rainbow tables) ⭐, "statelessness" u tokenu (chytré pochopení) ⭐, mantra OAuth × OIDC ("co smíš × kdo jsi") ⭐. Q1 OAuth: 4 aktéři + flow správně. Q2 refresh token: ~30 dní → generuje nový JWT bez hesla. **⚠️ Faktická chyba k drillu: JWT není šifrovaný!** User řekl "pro přenášení utajených dat, odolný vůči MITM". Správně: **JWT je base64-encoded, ne šifrovaný** (jwt.io payload čte kdokoli). Hodnota je v **podpisu** — chrání proti manipulaci, NE proti odposlechu. Proti odposlechu chrání HTTPS. **Drill před zkouškou:** 1) JWT = base64 + signature, ne šifrované. 2) bcrypt/Argon2 jako konkrétní hashing (NE MD5/SHA-256 — rychlé = špatné na hesla). 3) "Aplikace nikdy nevidí heslo" jako klíčová OAuth bezpečnostní vlastnost. 4) Refresh token = httpOnly cookie + revokovatelný. |
| 21 | RESTful | 1 | 7 | ✅ | 2026-05-13 | Heslovitý + 2 follow-upy (Q3 JSON-psaní zrušeno — komise u SWI ústní nechce psát kód/syntax, jen koncepty). Pokrytí 8/8 bodů Popisu: AJAX (Fetch API, async, historie Gmail/Maps), REST (URL + HTTP + stateless), JSON (6 datových typů kompletně), XML (legacy), chybové kódy (smart odkaz na SWI 19), Web API, CRUD-metody (POST/GET/PUT/PATCH/DELETE správně). Q1 stateless: "izolované requesty bez paměti serveru" ✅. Resource = zdroj (minimální, ale OK). Q2 všech 5 metod správně. **K doplnění při re-drillu:** 201 Created (po POST), 422 Unprocessable Entity (sémantická validace × 400 syntaktická), idempotence metod (GET/PUT/DELETE ano, POST ne), CORS + preflight OPTIONS, Authorization header pro auth tokeny (NIKDY v URL). REST URL konvence: plurál + path id + sloveso v metodě (NE /getUsers). |
| 22 | ASP.NET | 2 | 7 | ✅ | 2026-05-16 | **Téma odemčené Sokratovským dialogem.** User na start "nechápu to vůbec, jak v jiném jazyce" — místo lístku jsme šli ~10 otázek od základu (request → Kestrel → middleware → PageModel → handlery → routing → DI → lifetime). Po dialogu lístek pokryl 6/6 keywords. Pre 2 → post 7/10. **Silné momenty:** 4 typy aplikací rozlišené, async princip správně, Razor syntax kompletní (`@`/`@{}`/`@:` — pamatoval i edge case), User Secrets pro sekrety ⭐, reverzní proxy + TLS ⭐, file-based routing pochopen ⭐. **Lifetime všechny 3 + příklady správně** (cache=Singleton, DB=Scoped, mailer=Transient). **Drill body:** 1) "Kestrel" jako explicit klíčové slovo HTTP server (řekl "server ho přijme"). 2) DI ≠ konfigurace (zaměnil je — DI = constructor injection, samostatný koncept od appsettings.json). 3) Web API = JSON, Razor Pages = HTML, Blazor = C# místo JS. 4) Pojmenované handlery (OnPostSave, asp-page-handler) chybí. 5) Scoped pro DB = izolace stavu (transakce), ne primárně výkon. 6) Endpoint se nevygeneruje, ale **najde** routingem. |
| 23 | Událostmi řízené prog. | 1 | 7 | ✅ | 2026-05-18 | Heslovitý + 3 follow-upy. Pokrytí 5/5 keywords Popisu kompletní. **Silné momenty:** **Observer × Pub-Sub** distinkce přesná (1:N rychlejší UI × M:N přes brokera mikroslužby), **loose coupling** ⭐ "publisher neví co dělá, subscriber neví odkud přišel", memory leak řešení (unsubscribe), C# event keyword "mimo třídu jen += a -=", anonymní funkce chyták v JS ⭐ ("nefunguje, je to anonymní funkce, jiná reference"). **Drill body:** 1) **Event delegation** — listener na rodiči, `e.target`, funguje pro dynamicky přidané (využívá bubbling). 2) Delegát = **typesafe** ukazatel. 3) .NET handler konvence `void Handler(object sender, EventArgs e)`. 4) Capturing třetí parametr `addEventListener(..., true)`. 5) Event Loop (microtask vs macrotask) bonus. 6) AbortController moderní cleanup. |
| 24 | Programovací jazyky | 4 | 7.5 | ✅ | 2026-05-18 | Heslovitý + 3 follow-upy. Pokrytí 5/5 keywords. **Silné momenty:** **CLR/IL/JIT** komponenty .NET kompletně (papírek měl jen 3 verze, doplnil v Q1) ⭐ srovnání s JVM, **JIT × AOT trade-off** perfektně ("JIT optimalizuje pro aktuální HW, AOT rychlejší start"), **procedurální × deklarativní** přesná definice, **SQL jako deklarativní** ⭐ ("píšeme CO a odkud, ne JAK"), hybridní jazyky popsány. **Papírkové drobnosti k opravě:** 1) **BASIC NENÍ nízkoúrovňový** — je vysokoúrovňový (Beginner's All-purpose Symbolic Instruction Code z 60. let). Nízkoúrovňové = Assembler + strojový kód. 2) **TypeScript NENÍ čistě interpret** — transpiluje se na JS přes `tsc`, pak JS interpretuje (lépe: Python, PHP, Ruby, JS). 3) Mono umožnil C# i na iOS, Linuxu, Androidu. **Drill:** 4) **BCL = Base Class Library** (String, List, Console, File, Math) — user vágně "knihovna nebo něco". 5) JIT překládá **per metoda lazy** při prvním volání. |
| 25 | Architektura Android | 1 | 7 | ✅ | 2026-05-21 | Heslovitý + 3 follow-upy. Pokrytí 6/6 bulletů lístku kompletní. **Silné momenty:** 5 vrstev architektury plynule (Linux Kernel → HAL → ART+native → Framework → Apps); **profile-guided AOT** s konkrétním příkladem ("v noci při nabíjení překompiluje hot paths") + trade-off pomalejší instalace ⭐; **screen overlay** jako konkrétní special permission ⭐; manifest "**bez registrace komponenta NESPUSTÍ**" ⭐; ViewModel přežije rotaci ⭐; **Q1 Binder IPC** intuice perfektně ("komunikace mezi procesy + zachování izolace paměti") ⭐. **Faktické chyby k drillu:** 1) **❌ LIFECYCLE POŘADÍ** zaměnil — řekl `onCreate → onStart → onPause → onResume → onStop → onDestroy`. Správně `onCreate → onStart → onResume → onPause → onStop → onDestroy`. **Resume PŘED Pause** (Resume = stav interakce, Pause = částečně viditelný kvůli překryvu). 2) **DEX kompilace obráceně:** řekl "ART kompiluje do DEX" — správně ART **bere hotové DEX** (vzniká přes **D8/R8 dexer** ze .class) a kompiluje na strojový kód CPU. 3) **Chybí 4. kategorie permissions: Signature** (apps se stejným podpisem, Samsung apps mezi sebou). 4) **Google + OHA**, ne Alphabet (Alphabet je holding). 5) **Q2 Intent Chooser flow** — Chooser se objeví když **víc kandidátů + žádný default**, ne "když to nejde" (0 kandidátů = `ActivityNotFoundException`). 6) Compose **nemá vestavěnou MVVM**, MVVM je doporučená Google architektura pro celou aplikaci. **Drill body:** 1) Resume PŘED Pause. 2) D8/R8 → DEX, ART → strojový kód. 3) 4 kategorie permissions: Normal/Dangerous/**Signature**/Special. 4) ActivityNotFoundException při 0 kandidátech, Chooser při N>1 bez defaultu. 5) Runtime permissions od Android 6.0. **Q3 state hoisting** — propojení s React lifting state up (DAT 22) povrchně ("v Reactu useState"), nerozvedl, ale chápe propojení ⭐. |

---

## DAT (25 témat)

| # | Téma | Sebehodn. (před) | Sebehodn. (po) | Status | Poslední revize | Poznámka |
|---|------|---|---|---|---|---|
| 1 | HTML5 a sémantika | 5 | 8 | ✅ | 2026-05-09 | 4 mikroúlohy + finální stránka. Sémantická hierarchie čistá (header→nav, main→article→sections+aside, footer), figure refactor správně (img+figcaption bez redundance), form s fieldset+legend a všemi label-for/id páry. CSS typografie linknutá, font fallback chain, rem hierarchie. Pamatovat: legend bez dvojtečky, alt × figcaption oddělit (alt = popis pro slepé, figcaption = kontext pro všechny), česká typografie chce mezeru u `10 %`. Materiály v `_materials/dat/01/` neexistují → notes ⚠️ z obecných znalostí. |
| 2 | Bootstrap | 3 | 7 | ✅ | 2026-05-12 | **Pivot z fill-in-blank na from-scratch mini-tasks** (uložil productive_struggle do memory). 5 sekcí z prázdného souboru: navbar, hero, card grid (s moderní `row-cols-1/md-2/lg-3` syntax), form, 3-col footer. **Bonus moment:** našel `row-cols-*` lookupem (lepší než col-X-Y pro uniformní grid). Drobnosti: form `<div>` chybí `col` class + `mb-3`, `<textarea>` nemá `type=`, `card-title` patří na `<h5>` ne `<p>`. Pamatovat klíčové třídy: container/container-fluid, row/col, navbar-expand-lg/navbar-dark, card/card-img-top/card-body/card-title/card-text, form-control/form-label, btn-primary/btn-success, bg-dark/text-light, mb-3/py-4/g-3. |
| 3 | Flexbox | 5 | 8 | ✅ | 2026-05-09 | Validated assignment 5 modulů hotový. Vlastní extras u Modul 4 (.pricing flex-wrap, gap, align-items stretch) přidány pro responzivitu = obhajitelné. Past s "mezi nimi" v Modulu 1 — text dvojsmyslný, řešení komise = jen actions má margin-left: auto, ale alternativní interpretace (oba mají) je obhajitelná. U zkoušky drž referenci, mít alternativní argument pro doptání. Pamatovat: `min-width: 0` na flex-grow potomky s textem (anti-overflow), `flex-basis: 250px` u stretchujícich karet (rovnoměrná základní šířka). |
| 4 | CSS Grid | 5 | 8 | ✅ | 2026-05-10 | Validated assignment "holy grail" layout zvládnut. Mobile-first přístup (default mobile, media query desktop) — obhajitelný, defendovat jako "moderní praxe". Dva opravené bugy: `height: 100vh` → `min-height` (assignment chce "minimálně"), boundary `>=768px` → `>768px` (assignment: 768 = mobil), inkonzistence area names "asd" vs "aside". Pamatovat: **každý název v grid-template-areas MUSÍ mít matching grid-area na položce**, jinak duch-oblast. Modern range syntax `(width > 768px)` funguje, ale `(min-width: ...)` má lepší browser compat. |
| 5 | Pozicování + z-index | 5 | 8 | ✅ | 2026-05-11 | Validated assignment hotov: sticky header, float box, relative+absolute badge, fixed modal. Stylistické nuance vs validated (z-index 1 vs 67/69, badge přesně v rohu vs přesah, modal flush vs 20px margin) — obhajitelné. Bonus: semi-transparentní header `#333333d8`. 3 komise Q's vyřešené konceptualně: relative kotva pro absolute, sticky NESMÍ mít předka s overflow:hidden, **stacking context triggry** (transform, opacity, filter, isolation) — `transform: scale(1)` past klasická realná chyba. Pamatovat: doporučená hierarchie z-index (sticky 10-100, modals 1000+). |
| 6 | CSS animace | 2 | 7 | ✅ | 2026-05-11 | Validated 5-task assignment (karty hover, spinner, notifikace, btn, accessibility). Pre 2 → post 7 = velký skok. **Highlight: prefers-reduced-motion provedl sofistikovaně** — neúplné vypnutí, ale náhrada motion za color change (UX best practice). Bake delay do keyframes (alternativa k animation-delay). Drobnosti: `.btn:active scale(1.1)` opačně (konvenčně `0.95` = stisk dolů), magic number `-2000px` místo `-100%`, redundantní 0/50/100 ve spinneru, nekonečná wiggle ikona rušivá UX. Pamatovat: animation shorthand pořadí `name duration timing iteration`, `forwards` kritický když final keyframe ≠ default CSS state. Materiál byl "šedé" (typos v DOCX), obsah ale solidní. |
| 7 | Tabulky v HTML | 5 | 8 | ✅ | 2026-05-11 | Validated "Konference WebDev 2025" — full table s colspan/rowspan/colgroup, BEM třídy. **Bonus accessibility:** `scope="col"` v thead + `<th scope="row">` pro čas (sémanticky LEPŠÍ než reference, která má `<td>`). Drobnosti: chybí `</body>`, typo 13:00-14:30 (správně 14:00), CSS missing italic na break, display:block na span. **Meta-pozorování uživatele (správné):** zadání DAT 7 je outlier — testuje 30 % tabulek + 70 % obecného CSS designu (gradient, box-shadow, container). Strategie u zkoušky: HTML perfektně (5 min) + tabulkové CSS (15 min — col backgrounds, colspan, events) + polish if time. NESNAŽIT SE pixel-perfect matchovat obrázek. |
| 8 | Datové typy a pole | 2 | 7 | ✅ | 2026-05-09 | 5 mikroúloh + finální 4-section task za 40 min. Pamatovat: **pole je REFERENČNÍ typ** (variable drží adresu, obsah žije na heapu), ne hodnotový — vlastní pozorování "změna se projeví venku" měla být klíč. Drobnosti: ASCII v identifikátorech (ne `Pondělí`), labely sekcí ve výstupu (`Matice:`, `Trojúhelník:`), `i, j` místo `x, y` pro matrix indexy. Single-pass diagonála chytrá, drill-worthy strategie pro lookup vs drillování. |
| 9 | Spojové struktury, stromy | 1 | 5 | 🟡 | 2026-05-13 | 5 mikroúloh: Node class, manuální 3-uzlový seznam + traversal, LinkedList (Add/Print/Contains/InsertAfter), BST (Insert + InOrder rekurzivně). Pre 1/10 → post 5/10. **Princip pochopen**, ale plnohodnotná 30-min from-scratch implementace všech metod (Min/Max/Height/Count) borderline. Pamatovat: 1) traversal pattern `while(current != null) { ...; current = current.Next; }`. 2) Insert vrací modifikovaný uzel (kvůli `node.Left = InsertRec(...)`), traversal je `void`. 3) Single-pass InsertAfter. 4) BST O(log n) jen vyvážený; seřazený vstup → O(n) degenerace. Re-drill ~2026-05-17. |
| 10 | Podprogramy a lambda | 2 | 8 | ✅ | 2026-05-08 (2× drill + 2× praxe) | 2 praktické úlohy (SoucetCifer; TryNacti+PridejValidni+Spocitej — `out` z producent i konzument strany, HOF s lambdou). Ref-type passing pochopeno přes metaforu lístku/domu: **mutace** objektu prosakuje ven (`Add`), **přepsání** reference ne (`= new List`) — to by chtělo `ref`. Drobnosti k zapamatování: čistší if syntax (bez `== true`), funkce vs procedura — obě mají parametry, rozdíl je v návratové hodnotě; scope funkční = celé tělo funkce (ne parametr); v odpovědi o lambdě vždy doplnit konkrétní příklad (LINQ Where). |
| 11 | Kolekce | 3 | 8 | ✅ | 2026-05-10 | 5 mikroúloh + finální Bistro task. Stack (LIFO Push/Pop/Peek) ✅, Queue (FIFO Enqueue/Dequeue/Peek) ✅, Dictionary (Add/[]/ContainsKey/TryGetValue/iterace) ✅, counting pattern přes TryGetValue (efektivnější) ✅, kombinace všech 4 v Bistru ✅. **Self-debugged Peek bez závorek** — method group vs metoda volání (stejná lekce z DAT 10 mikroúlohy 11 s `SoucetCifer`). Pamatovat: TryGetValue je 1 lookup, ContainsKey+[] jsou 2 lookups. Drobnosti v Bistru: chybí "Kč" suffix v závěrečných řádcích, typo Hranolky 150 vs 60 (nezasáhlo výstup, ale "číst zadání precizně"). |
| 12 | Souborový systém + streamy | 4 | 8 | ✅ | 2026-05-20 | **Leaked zadání + 7 mikroúloh** v `_practice/dat12-soubory/`. Splnil všechny 3 sekce zadání: 1) Procházení Data + počty + velikost (84 B). 2) Čtení textových souborů + zápis Output/jmena.txt s 7 jmény + re-čtení. 3) Output/byte.bin s 10 random bajty + re-čtení. **Silné momenty:** Directory.GetFiles + AllDirectories ✅, FileInfo.Length sum, File.ReadAllText pro malé soubory, **AddRange pro slučování** řádků z víc souborů ✅, Random.NextBytes + File.WriteAllBytes + ReadAllBytes pro binární data. Defense Q&A: streamy + buffer + using, binární × text (PNG magic 89 50 4E 47), multiplatformní cesty (Windows přijímá `/` i `\`!) ⭐. **Drobnosti k drillu:** 1) Streamy = paměťová efektivita (sekvenční vs vše-do-RAM, ne "blíže k hw"). 2) Buffer = mezi-paměť v RAM, Flush = spláchnutí. 3) Magic number v hlavičce, ne koncovka. 4) Path.Combine pro 100% multiplatformní. 5) Directory.Delete na neexistující = crash, vždy if Exists před. 6) ReadAllText vs ReadAllLines + AddRange. **Chyby učené postupně:** StreamReader("Data") = otevřít složku jako stream → UnauthorizedAccess. Directory.Delete bez Exists = crash. File.ReadAllText do List + WriteAllLines = 3 multiline položky místo 7 řádků. |
| 13 | Paralelní/async programování | 1 | 6.5 | ✅ | 2026-05-20 | Praktika v `_practice/dat13-async/` — hledání max v 100M poli sekvenčně vs paralelně + měření. M1-M4 kompletní + **BONUS** `cisla.Max()` (SIMD-accelerated LINQ, 37 ms vs 50 ms paralelně) ⭐. **Silné momenty:** Task.Run + Task.WhenAll správně, lokální kopie start/end před Task.Run (closure protection) ✅, NajdiMaxVCasti pomocná metoda, double cast pro zrychlení. Defense: **Q2 closure perfektně** ⭐ ("zachytí referenci, ne hodnotu"), Q3a race condition správně. **❌ Slabý moment Q1b/c:** async/await × Task.Run **pomícháno**. User dal Task.Run definici i pro async/await. **Drill body:** 1) **MANTRA: Task.Run na výpočty, await na čekání.** 2) async/await NEVYTVÁŘÍ vlákno, uvolní stávající pro I/O. 3) Task.Run vytvoří vlákno z ThreadPool pro CPU. 4) `suma += x` ne-atomická (3 kroky čti/přičti/zapiš). 5) Race condition řešení: **lock + Interlocked (rychlejší) + thread-local state v Parallel.For** (nejrychlejší). 6) Closure zachytí referenci. 7) **SIMD-accelerated LINQ** — `Max/Min/Sum` nad primitive arrays je vektorizováno. **User pushback:** `Environment.ProcessorCount/2` je validní (fyzická jádra) — komise akceptuje. |
| 14 | Git a GitHub | 5 | 8 | ✅ | 2026-05-10 | 5 mikroúloh + merge conflict resolution. Init/add/commit/log ✅, branching (switch -c, izolace) ✅, fast-forward merge ✅, .gitignore + past s už trackovanými soubory + `git rm --cached` ✅, **merge conflict** s manuální resolution ✅, `--graph` Y-struktura. Pamatovat: gitignore filtruje JEN untracked, na trackované soubory už nepůsobí. `git merge --abort` pro zrušení rozdělaného mergu. Default branch master (starší config), moderní = main. |
| 15 | ER model | 2 | 7 | ✅ | 2026-05-19 | Praktická úloha — ER diagram v draw.io SVG (`_practice/dat15-knihovna/knihovna.svg`) pro knihovnu. **Splnil všechny požadavky:** M:N Kniha-Autor přes vazební tabulku ⭐, M:N Čtenář-Kniha přes Výpůjčka **s surrogate LeaseId** ⭐ (umožní opakované výpůjčky), rekurzivní Knihovník.SupervisorId ✅, 1:1 Sekce-Vedoucí (přes vazební tabulku — over-engineering, ale funkční). **Silné momenty:** **sebereflexe v Q1b** ⭐ ("můj přístup nemá výhody, FK UNIQUE by bylo lepší"), uvědomění problému SET NULL na Výpůjčka v Q3 ⭐. **Drobnosti k drillu:** 1) **Surrogate keys best practice** — user dal Email/ISBN jako PK, ale Google/MS/FB všichni používají interní user_id (ne email). 2) **Vazební tabulka Kniha-autor: CASCADE, ne SET NULL** (jinak porušíš PK NOT NULL — složený PK obsahuje AutorId). 3) **Kniha → Výpůjčka: RESTRICT je idiomatic** (zachovat historii), ne CASCADE. 4) Cascade update problém při změně PK (email → all FKs update). 5) **1:1 přes FK + UNIQUE**, ne vazební tabulku. 6) Rekurzivní FK SET NULL pro hlavního knihovníka ✅. |
| 16 | SQL výběr a filtrování | 5 | 6.5 | ✅ | 2026-05-19 | Praktická úloha — exam-style final v `_practice/dat16-sql-vyber/` nad `exams.sqlite` (5 tabulek, 18 studentů, 136 hodnocení). User dal 8/8 úloh. **5/8 OK** (úlohy 1, 2, 4, 6, 7), **3/8 chyby:** Úloha 3 chybí concat (jméno+příjmení jako jeden sloupec přes `||`), **Úloha 5 ❌ JOIN s Trida místo Predmet** (vrátil název třídy, ne předmětu, chybí ORDER+LIMIT), **Úloha 8 ❌ broken syntax** (`GROUP BY ... AND ORDER BY` = invalid), **chybí HAVING** (filter "nad 75"), chybí JOIN s Predmet. **Drill body:** 1) HAVING pro filter skupin (zadání "nad X" / "víc než Y" → HAVING, ne ORDER BY). 2) JOIN s **správnou tabulkou** podle zadání (Predmet × Trida). 3) GROUP BY + SELECT — všechno musí být v GROUP BY nebo agregace. 4) Žádné AND/OR mezi klauzulemi. 5) Concat v SQLite `||`. 6) **Před odevzdáním pomalu projdi dotaz** — odpovídá zadání slovo po slově? Tempo OK (8 úloh / 30 min), kvalita kontrola chybí. **Plus:** prošli jsme i mikroúlohy 01-jednoduche (4/5 OK), 02-where (původně 2/6, po opravě všech 5 ✅). Hlavní lekce z 02: **JOIN potřebuje ON** + **JOIN nepotřebuju když mám FK** + **IN > OR řetězec** + **AND > OR priorita** (vždy závorky nebo IN). |
| 17 | REST API v ASP.NET | 2 | 6 | 🟡 | 2026-05-17 | Leaked attack — MovieClubPractice baseline (EF Core + SQLite + Movie model) + napsání MoviesController s 8 endpointy. User dokončil 7/8 (vote endpoint chybí). **Kostra controlleru solidní:** [ApiController], [Route("api/[controller]")], DI DbContext, ActionResult<T>, status helpers (Ok/NotFound/CreatedAtAction/NoContent). **GET (All, ById, Unwatched) + PUT** plně funkční. **❌ 2 kritické SaveChanges bugy = data se neuloží do DB:** 1) PostMovie chybí `_db.Movies.Add(movie)` + `_db.SaveChanges()` — vrátí 201 ale film v DB neexistuje. 2) DeleteMovie chybí `_db.SaveChanges()` + používá parametr `movie` (null) místo `existing`. **Drobnosti:** GetAll bez filtrů ?genre/?addedBy, GetMostVoted bez filtru !IsWatched + bez tie-break + .First() místo .FirstOrDefault. **Mantra k zafixování:** "Každá změna (Add/Update/Remove) v EF Core musí být následována SaveChanges". **Status 🟡** — re-drill v ASP.NET revizi ~2026-05-22 (memory: project_aspnet_review_before_maturita). User: "víc nezvládnu" — vyčerpání po náročném Dni 9. |
| 18 | Razor Pages | 1 | 8 | ✅ | 2026-05-17 | Sokratovský warm-up (kontaktní formulář v `_practice/dat18-warmup-contact/`) + leaked attack (registrace kurzu v `_practice/dat18-razor-pages/`). Pre 1 → post 8/10. End-to-end aplikace funguje, všech 6 keywords pokryto (handlery, binding, návratové metody, TempData, routing, validace). **Silné:** pretty URL pattern `@page "{email?}"` + `[BindProperty(SupportsGet=true)]` aplikován správně (warmup díra opravena) ⭐, PRG pattern dodržen, Kurzy re-load při chybě idiomatic, OutputModel + InputModel konzistentní napříč stránkami. Defense Q&A: pretty URL edge cases ✅, Email validace ✅⭐ (sám identifikoval chybějící EmailAddress), SupportsGet princip ✅. **Drill body:** 1) validation span pro KAŽDÉ pole (chyběl pro Kurz). 2) [EmailAddress] vedle [Required]. 3) Email idiomatic v InputModel (jeho přístup mimo InputModel funguje, ale je hacky). 4) **Validace běží JEN v OnPost** přes ModelState.IsValid, ne automaticky v OnGet. 5) Pretty URL = dva kroky (`@page "{x?}"` + `[BindProperty(SupportsGet)]`). User: "logika není složitá, ale syntaxe je hodně" — DAT zkouška docs allowed. |
| 19 | Tag Helpers + formuláře | 1 | 7 | ✅ | 2026-05-17 | Leaked attack — scaffold s chybějícími Tag Helpers (4 stránky: Index/Subscribe/List/Detail) + in-memory SubscriptionStore přes DI Singleton. User doplnil: asp-for + asp-items + asp-validation-for (Index, Subscribe), validační atributy InputModel ([Required], [EmailAddress]), asp-page-handler + asp-route-id (List). **Klasický bug v Index:** `asp-for="AvailableCourses"` (seznam) místo `asp-for="SelectedCourse"` (target property). Mantra: *"asp-for = kam uložit, asp-items = co nabídnout"*. Po opravě end-to-end funguje. Defense Q&A: **Q1 4/10** — popletl name vs id (name=binding, id=label-pair), vynechal data-val-* atributy. **Q2 5/10** — myslel že s JS off POST nepřejde, ale POST se stane, server validuje sám (single source of truth). **Q3 7/10** — handler routing pochopen, mechanismus query param `?handler=X` chybí. **Drill body:** 1) name × id v asp-for. 2) data-val-* = klientská validace. 3) S JS off klient stejně POST udělá. 4) asp-page-handler → query param. 5) Klasický bug asp-for na list vs selected. **User feedback k procesu:** "kámo tam nechybí Tag Helpers, tam chybí všechno" — scope leaked = doplnit do scaffoldu, ne stavět od nuly. |
| 20 | Next.js (SSR/CSR) | 1 | 7 | ✅ | 2026-05-23 | **80% hotovo, Dashboard+Clock chybí, dokončit v sobotním redrillu.** Leaked zadání ze spoluzaci-validovane (nextjs-maturita) — projekt `_practice/next-testovani/`. **Hotovo:** Setup `create-next-app` + TypeScript, **Navbar** s `next/link` (Server Component, Link href), **`app/layout.tsx`** wrap s Navbar nad children, **Homepage** (`app/page.tsx`, Server Component, žádný `"use client"`, 2 linky), **Blog dynamic route** (`app/blog/[slug]/page.tsx` — **async + `params: Promise<{ slug: string }>` + `await params`** — Next 15 pattern ⭐ trefa napoprvé z mé reference). **Chybí (sobota):** `components/Clock.tsx` (`"use client"` + useState + useEffect + setInterval + cleanup clearInterval) + `app/dashboard/page.tsx` (Server Component importující Clock). **User pochopení Server×Client ⭐:** *"s use client bych získal interaktivitu, prekladalo by se to client side, ale takhle to ma lepsi loading, protoze to prichazi jako ready html"*. **Drill body pro sobotu:** 1) `"use client"` PRVNÍ řádek souboru. 2) Server Component je default v App Routeru, nemá hooks. 3) Dynamic segment = složka v `[slug]` s hranatými závorkami. 4) Next 15+: `params: Promise<{ slug: string }>` + `await params` (Next 14 byl přímý objekt). 5) `<Link href="...">` z `next/link`, ne `to=` jako React Router. **U zkoušky kde hledat:** `_docs/nextjs.docs/docs/01-app/03-api-reference/02-file-conventions/page.mdx` (params) + `05-server-and-client-components.mdx`. Zeal NEMÁ Next.js docset (jen React). **User push-back na SHORCUTS.md** — uvědomil si, že u zkoušky repo nebude → memory `feedback_shortcuts_pre_exam_only.md`. **Den 15 končí druhým vyhořením** — vyhořel u DAT 25 → zkusil Next jako alternativu → po 80% Next znovu vyhořel. Konec dne, opravdu pauza. |
| 21 | ORM | 1 | 6.5 | ✅ | 2026-05-20 | **Prisma 7 + SQLite + TS praktika** v `_practice/dat21-orm/`. Začátek: user řekl "vysvětli mi to od začátku jak dítěti" — reset na **vysvětlení mentálního modelu** (co je ORM, EF Core × Prisma, soubory v projektu, workflow). Pak high-level zadání mini blog + reference syntaxe (per memory feedback_high_level_task). User postavil end-to-end: 1) schema.prisma se 2 modely (Autor + Clanek, 1:N vazba). 2) prisma migrate dev → dev.db + migration.sql + generated client. 3) lib/prisma.ts s PrismaBetterSqlite3 adapterem. 4) src/test.ts s 5 CRUD operacemi. **Silné momenty:** schéma kompletně validní (Prisma souhlasí), `connect: { id }` pro propojení relace ✅, include vrátí nested autor objekt ✅, **idempotentní deleteMany cleanup** ✅, použití id z `novyClanek.id` ne hardcoded ✅, select × include rozlišení ⭐. **Lekce z chyb:** 1) Pořadí create operací — nejdřív rodič, pak dítě (P2025 error: connect na neexistující záznam). 2) **Nereaktivní JS proměnná** — `clanekPodleId` snapshot, vypisovat návratovou hodnotu update/delete, nebo nový findUnique. 3) Po delete `findUnique` vrátí null (ne výjimka, jen null check). **Drill k defense:** Driver adapter = most Client↔DB driver (Prisma 7 odstranila Rust engine). migrate dev 4 věci: SQL, apply, migration soubor, regen klient. N+1 = 1+N dotazů, include řeší. dotenv/config načte .env do process.env. User-feedback uložen jako memory: vysokoúrovňové zadání + reference syntaxe, NIKDY field-by-field spec. |
| 22 | React komponenty | 3 | 6 | ✅ | 2026-05-21 | **Type-along celý den** — ráno full reset ("nic neumím, prázdno v hlavě"), večer funkční TodoList type-along stylem od nuly. Pokrytí konceptů: 4 komponenty (App/TodoForm/TodoList/TodoItem), typed props, 3 callbacky (onPridej/onSmaz/onHotovo), controlled inputs, immutabilní update (spread + map/filter), prop drilling, key. **Silné momenty:** šipkové funkce drill 4/4 trefil hned (zdvojnasob, secti, filter sudých, map dvojnásobků) — recognition vs recall gap **přemostěn praktikou**; Prisma analogie pro `.filter()` (`findMany where` ↔ `arr.filter`) **vlastní iniciativa** ⭐; intuice na `<label>` u checkboxu bonus (accessibility) ⭐; imutabilita pochopena přes "krabice + štítek" metaforu (Q5 v Q&A ✅). **Drill body:** 1) **Komponenta = funkce přijímající props + vracející JSX** (řekl jen "samostatná část tvařící se jako tag" + atomic design metafora — chybí technické vymezení). 2) **State × props:** state = interní paměť měnitelná setterem, props = vstupní argumenty od rodiče **immutable z perspektivy dítěte**. 3) **Lifting state up = nejbližší společný předek**, nejen "abstraktně sdílená reference". 4) **❌ KLASICKÝ CHYTÁK** (řekl OBRÁCENĚ Q4): **Data v Reactu tečou SHORA DOLŮ** (unidirectional). Callbacky předané dolů + volané v dětech = info "nahoru". Ne data od dětí. 5) Imutabilita ✅ "porovnáváme reference ne hodnoty" ⭐. 6) `key` ✅ unikátní identita prvku, bez něj React zmaten. **Recall gap reset:** ráno přepnut na type-along (já píšu, on doplňuje ???-y), večer dokázal `submit()` funkci v TodoFormu napsat celou sám. **Memory:** [[feedback-swi-listek-minimalni]] (škála 1-10), [[feedback-high-level-task]] (pro praxi až později po type-along). |
| 23 | React hooks | 3 | 7 | ✅ | 2026-05-22 | **Type-along rozšíření TodoListu** z DAT 22 o všechny 4 hooky + cleanup. Pokrytí: `useState` (ukoly, loaded, text), `useEffect` 3× (load z localStorage, save do localStorage, document.title), `useRef` (auto-focus inputu v TodoForm), `useId` (label-input pár). **Silné momenty:** **race condition s `loaded` flag** pochopen ⭐ ("dep array = KDY se spustí, podmínka uvnitř = CO když, sama o sobě nerozhoduje" — vlastní formulace v Q6); šipková funkce v filter (`u => u.hotovo`) bez `=== true` — drobnost stylistická; useRef objasněn ⭐ ("uchovává hodnotu, kterou nechceme znovu renderovat"); cleanup demo na timeru spuštěn live a viděl Strict Mode double-mount v Console. **Drobnosti k drillu:** 1) `u.hotovo === true` → `u.hotovo` (boolean nemusí === srovnání). 2) **Try-catch obráceně** (počáteční pokus): `JSON.parse` byl MIMO try, `setUkoly` UVNITŘ — opraveno. **Faktické chyby v Q&A k drillu:** 1) **❌ Infinite loop ≠ chybějící cleanup** (Q1). Infinite loop = `setState` uvnitř useEffect s vadným dep arrayem → každý render triggeruje effect → setState → re-render. React: `Maximum update depth exceeded`. 2) **Cleanup spouštění:** **deps change** NEBO **unmount** — ne "ukončení funkce". 3) **document.title useEffect** běží **při každé změně `ukoly`**, ne jen 1× (má dep `[ukoly]`). Cleanup nepotřebuje protože title je **idempotentní přepsání**, ne running process. 4) **Duplicate ID** = HTML/a11y violation, **ne crash aplikace**. 5) **3 pravidla hooks** chybělo v Q1: top-level only, jen z komponent/custom hooks, stejné pořadí každý render. 6) **Q3 fetch + cleanup** — user řekl "nevim". Pattern **AbortController v cleanup** (`controller.abort()` v `return () => ...`) řeší stale state update bug + race condition při rychlé navigaci. **Bonus:** Lazy initializer pattern `useState(() => loadFromStorage())` jako čistší alternativa k mount-load přes useEffect — komise to ocení. **TodoList plně funkční** v `_practice/dat22-todolist/` — používá se i pro DAT 22 obhajobu. |
| 24 | React Router | 3 | 6 | ✅ | 2026-05-22 | **Type-along + samostatný úkol.** Rozšíření TodoListu z DAT 22/23 o React Router 6: BrowserRouter v main.tsx (obal), Routes/Route + path="*" wildcard, Link (vs `<a>`), useParams (s `Number(id)` konverze ze stringu), useNavigate (nav("/")  pro post-action, nav(-1) pro back). Architektura: App drží state ukoly + Routes, pages/ má Home/About/NotFound/UkolDetail/Statistiky, props předávány do pages. **User push-back uprostřed cvičení** ⭐: *"JA NIC NEVIM, TOHLE NENI OPAKOVANI, JA SE TO MAM DNESKA NAUCIT"* — důvod: já napsal většinu kódu (Home/About/NotFound/UkolDetail), hodil hotové, pak chtěl in-depth Q&A. **Pedagogická chyba ode mě, opraveno**: vytvořil zápisky `_notes/dat/24-react-router.md` + dal user samostatnou úlohu (`/statistiky` page). **Silné momenty:** samostatná Statistiky page od nuly ⭐ (props, filter hotových/nehotových, conditional procento, Link zpět). **Drill body (chyby v samostatné Statistiky):** 1) **❌ Derived state anti-pattern** (`useState` + `useEffect` pro `procentoHotovych` které je odvoditelné z props) — počítej derived v render, ne v state. Pravidlo: *"pokud hodnotu můžeš vypočítat z props nebo jiného state, NEDEJ ji do state"*. 2) **`<Link to="statistiky">` chybí lomítko** — relativní URL, na `/ukol/5` → `/ukol/5/statistiky` = 404. Vždy `/` na začátku. 3) **`Math.round(... / ...)*100`** — Math.round na podílu (0-1) → vždy 0 nebo 100%. Musí obklopit celý výraz `Math.round((a/b)*100)`. 4) **`var` v JS = legacy** (function scoping, hoisting). C# `var` = type inference (úplně jiný význam). V JS/TS: `const` default, `let` pro reassign, `var` NIKDY. 5) Unused imports — ESLint warning. **User feedback:** preferuje if/else před ternárem (validní stylistika). |
| 25 | Správa stavu v Reactu | 2 | 5 | 🟡 | 2026-05-22 | **70 % hotovo, dokončit v sobotním redrillu.** Zápisky `_notes/dat/25-sprava-stavu.md` ✅ čteny. **Praktika v TodoListu** — reducer napsán od nuly (3 cases ADD/TOGGLE/DELETE + 4. INIT pro localStorage load), useReducer nahradil useState s `loaded` flag pro race condition. Context vytvořen v `UkolyContext.tsx` (createContext, custom hook `useUkoly` s null check + throw). **Nedokončeno:** Provider wrap v App + refactor pages (Home/UkolDetail/Statistiky) z props na useContext. **Leaked zadání e-shop košík** (`_practice/dat25-eshop/` skopírováno ze spoluzaci-validovane) — TODOs neudělány, mozkové vyhoření po 5h dni. **Drill body pro sobotu:** 1) Reducer = **čistá funkce** (state, action) → newState. 2) Context **nedrží state**, je trubka. Reducer/useState drží, Context propaguje. 3) Discriminated union pro Action type — TS pak ví, že u 'ADD' je action.payload jiné než u 'DELETE'. 4) Custom hook `useXxx()` jako wrapper nad `useContext` + null check. 5) Imutabilita v reducer stejně jako v setState (spread/map/filter). **User vyčerpání** — Day 15 byl intenzivní (DAT 23 + 24 + DAT 25 částečně + 4 zápisky + plán + 5+ hodin kódu). |

---

## ČJL (20 knih)

| # | Titul | Autor | První pass | Poslední revize | Status | Poznámka |
|---|-------|-------|-----------|-----------------|--------|----------|
| 1 | Romeo a Julie | Shakespeare | 2026-05-09 | 2026-05-09 | 🟡 | Papírek 5/10. Faktická chyba: Merkucio = přítel, NE Romeův bratranec. Pojmový zmatek "jamb = forma promluvy" (jamb je metrum). Kostra OK. Díry: kontext autora (raná tragédie, italský námět z Bandella), 5 jednání + prolog-sonet Chóru, blankvers (≠ jen "jamb"), oxymóron jako signature prostředek (uměl definici, ne příklad), Romeův EXIL do Mantovy, nedoručený dopis. Před zkouškou re-pass body 6, 8, 11 z rozboru. |
| 2 | Lakomec | Molière | 2026-05-09 | 2026-05-09 | 🟡 | Papírek 6/10. Klasicismus pevně (3 jednoty, vysoký/nízký žánr, Plautus/Aulularia po doptání ✓). V úryvku gradace + řeč. otázka + hyperbola ✓; **expresivní slovo "lotře"** ⭐ samostatný nález. Plot tvist převrácený: Šipka ukradne truhličku → **Kleant** ji použije pro Marianu (NE pro Valéra+Elišku); Anselm = **deus ex machina** vyřeší Valéra+Elišku. Díry: časoprostor úplně, postavy Šipka/Čipera/Jakub, **apostrofa × personifikace** (rozlišit), próza jako výjimka v tvorbě Molièra po doptání ✓. Další zástupci klasicismu (Racine/Faidra, Corneille/Cid, La Fontaine/Bajky) chybí. |
| 3 | Revizor | Gogol | 2026-05-09 | 2026-05-09 | 🟡 | Papírek 4/10 — **nejslabší ze dne, k cílené revizi dřív než R&J/Lakomec**. Plot kostra ✓, "Chlestakovština" jako pojem ⭐, hyperbola+gradace+ironie v úryvku ✓ (formulace ironie *"hejtman vzhlíží k někomu, kdo mu nesahá ani po kotníky"* solidní pro zkoušku). **Faktické chyby:** kompozice "rámcová" → ve skutečnosti **chronologická, 5 jednání**; postak odhalí "konverzaci" → ve skutečnosti **dopis** Trjapičkinovi. **Klíčové díry (1–4 v doptání všechno minul, učební minimum předáno):** **němá scéna** ⭐ na konci, **Osip** (sluha chytřejší než pán), **Bobčinskij+Dobčinskij** jako spouštěč omylu, **Puškin dal Gogolovi námět** (perlička pro komisi). Místo: maloměsto/újezdní město, NE venkov. Komická jména postav (Skvoznik, Ljapkin-Ťapkin, Zemljanika) jako gogolovský prostředek úplně chybí. |
| 4 | Obraz Doriana Graye | Wilde |  |  | ❌ |  |
| 5 | Kytice | Erben |  |  | ❌ |  |
| 6 | Po nás ať přijde potopa | Gellner |  |  | ❌ |  |
| 7 | Deset malých černoušků | Christie |  |  | ❌ |  |
| 8 | Na západní frontě klid | Remarque |  |  | ❌ |  |
| 9 | O myších a lidech | Steinbeck |  |  | ❌ |  |
| 10 | Farma zvířat | Orwell |  |  | ❌ |  |
| 11 | 1984 | Orwell |  |  | ❌ |  |
| 12 | Sněhulák | Nesbø |  |  | ❌ |  |
| 13 | Krysař | Dyk |  |  | ❌ |  |
| 14 | Švejk (1. díl) | Hašek |  |  | ❌ |  |
| 15 | RUR | Čapek |  |  | ❌ |  |
| 16 | Bylo nás pět | Poláček |  |  | ❌ |  |
| 17 | Ostře sledované vlaky | Hrabal |  |  | ❌ |  |
| 18 | Spalovač mrtvol | Fuks |  |  | ❌ |  |
| 19 | Smrt krásných srnců | Pavel |  |  | ❌ |  |
| 20 | Vyšetřování ztráty třídní knihy | Cimrman |  |  | ❌ |  |

---

## Týdenní snapshot

Aktualizuj jednou týdně (např. neděle večer):

| Datum | SWI ✅/🟡/❌ | DAT ✅/🟡/❌ | ČJL | Dnů zbývá | Tempo (na den) | Poznámka |
|-------|-----|-----|-----|-----------|------|----------|
| 2026-05-07 | 0/0/25 | 0/0/25 | (user) | 18 | start | Setup day (Den 0). |
| 2026-05-10 | 4/1/20 | 7/0/18 | (user) | 15 | **3.7** ✅ (cíl 2.6) | **Předbíháš plán o ~1.1 slotů/den.** 11 ✅ + 1 🟡 ze 50. Block A téměř hotov (zbývá SWI 3, SWI 9 🟡, DAT 9). Block B rozjetý (5/12 hotových). Vibecoder fundament převeden — datové typy, funkce/lambda, kolekce dovedeš naprogramovat z hlavy. Re-drily fungují: SWI 2, SWI 4, SWI 16 prošly z 🟡 na ✅. Heslovitý lístek formát + komise Q&A potvrzeno jako efektivní validace. **Riziko:** OOP (SWI 9) je gatekeeper pro DAT 11 ★ DONE, DAT 22 React komponenty, ASP.NET témata. Re-drill plánován ~2026-05-14. |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |
